const { config } = require('../../core/config');

module.exports = {
    config: {
        name: 'menu',
        command: 'menu',
        aliases: ['قائمة', 'الاوامر', 'help'],
        category: 'general',
        description: 'عرض قائمة الأوامر الكاملة',
        usage: '.menu',
        cooldown: 5,
        ownerOnly: false,
        groupOnly: false,
        privateOnly: false,
        adminOnly: false
    },

    async execute(sock, messageInfo, args) {
        const { from, sender, isOwner } = messageInfo;
        
        try {
            const menuText = this.generateMenu(isOwner);
            
            await sock.sendMessage(from, {
                text: menuText,
                contextInfo: {
                    externalAdReply: {
                        title: `${config.bot.name} - القائمة الرئيسية`,
                        body: 'اختر أمرك من القائمة أدناه',
                        thumbnailUrl: 'https://i.ibb.co/5sZ2z2z/ayatech-logo.png',
                        sourceUrl: 'https://github.com/ayatech',
                        mediaType: 1,
                        renderLargerThumbnail: true,
                        mentionedJid: [sender]
                    }
                }
            });
            
        } catch (error) {
            console.error('Menu command error:', error);
            await sock.sendMessage(from, {
                text: '❌ حدث خطأ أثناء عرض القائمة'
            });
        }
    },

    generateMenu(isOwner = false) {
        const currentTime = new Date().toLocaleString('ar-MA', {
            timeZone: 'Africa/Casablanca'
        });

        let menu = `
╭───「 *${config.bot.name}* 」───
│🤖 البوت: ${config.bot.name}
│👤 المالك: ${config.owner.name}
│🇲🇦 البلد: ${config.owner.country}
│📧 الإيميل: ${config.owner.email}
│⏰ الوقت: ${currentTime}
╰─────────────────────

╭───「 *الأوامر العامة* 」───
│ .menu - عرض هذه القائمة
│ .ping - سرعة الاستجابة
│ .alive - حالة البوت
│ .help - المساعدة
│ .owner - معلومات المالك
╰─────────────────────

╭───「 *أوامر الذكاء الاصطناعي* 」───
│ .ai <رسالة> - محادثة ذكية
│ .ask <سؤال> - طرح سؤال
│ .gpt <نص> - تحليل نص
╰─────────────────────

╭───「 *أوامر التحميل* 」───
│ .yt <رابط> - تحميل من يوتيوب
│ .tiktok <رابط> - تحميل من تيكتوك
│ .ig <رابط> - تحميل من إنستغرام
│ .fb <رابط> - تحميل من فيسبوك
╰─────────────────────

╭───「 *أوامر الجروبات* 」───
│ .add <رقم> - إضافة عضو
│ .kick <مستخدم> - طرد عضو
│ .promote <مستخدم> - ترقية لمسؤول
│ .demote <مستخدم> - إزالة الإدارة
│ .mute - كتم المجموعة
│ .unmute - فك الكتم
╰─────────────────────

╭───「 *أوامر التسلية* 」───
│ .joke - نكتة عشوائية
│ .fact - معلومة مثيرة
│ .quote - اقتباس ملهم
│ .meme - ميم عشوائي
╰─────────────────────
`;

        if (isOwner) {
            menu += `
╭───「 *أوامر المالك* 」───
│ .broadcast <رسالة> - إذاعة
│ .block <مستخدم> - حظر
│ .unblock <مستخدم> - إلغاء الحظر
│ .leave <جروب> - مغادرة
│ .join <رابط> - الانضمام
│ .eval <كود> - تنفيذ كود
│ .restart - إعادة التشغيل
╰─────────────────────
`;
        }

        menu += `
╭───「 *معلومات إضافية* 」───
│ 🔰 البادئة: ${config.bot.prefix}
│ ⚡ سرعة الاستجابة: عالية
│ 🔒 الحماية: مفعلة
│ 🌐 الدعم: 24/7
╰─────────────────────

📌 *نصيحة:* استخدم .help <أمر> للحصول على تفاصيل الأمر
🔰 *مثال:* .help yt

© 2024 ${config.bot.name} - ${config.owner.name}
`;

        return menu;
    }
};
